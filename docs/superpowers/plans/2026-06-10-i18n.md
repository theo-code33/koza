# kōza — Internationalisation (i18n) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Câbler next-intl 4 en mode sans-routing (locale résolue par cookie `NEXT_LOCALE` → DB `UserSettings.locale` → défaut `fr`), extraire toutes les chaînes visibles vers `src/locales/{fr,en}.json` complets, et offrir un sélecteur de langue persistant.

**Architecture:** `getRequestConfig` résout la locale active à chaque requête. Le layout racine (Server Component) pose `<html lang>` et fournit `NextIntlClientProvider`. Les Server Components consomment `getTranslations`/`getLocale`, les Client Components `useTranslations`/`useLocale`. Un toggle (server action) écrit DB + cookie puis `router.refresh()`. Les formatters (déjà `locale`-aware) reçoivent la locale active.

**Tech Stack:** Next 16 App Router, React 19, next-intl 4, Prisma 7, Vitest + Testing Library, ICU message format.

---

## Dépendance base de données (lire avant d'exécuter)

`resolveLocale` et `setLocaleAction` lisent/écrivent `UserSettings` via Prisma. **Tous les tests mockent Prisma** (`vi.mock("@/lib/prisma")`) et `next/headers` — aucune connexion DB requise. Le build (`next build`) régénère le client via `postinstall` ; lancer avec un `DATABASE_URL` factice. La vérif manuelle (`npm run dev`, Task 16 Step 2) nécessite la DB dev du mainteneur.

## Conventions pour chaque commit

- Branche : **`feat/i18n`** (déjà créée depuis `main`). Jamais de commit sur `main`.
- Conventional Commits, anglais, ≤72 car., scope `i18n` (ou `settings`/`lib`/`db` selon le fichier dominant).
- Aucun trailer `Co-authored-by`. Merge commit, pas de squash. PR via `gh`, merge manuel.
- Avant chaque commit : `npm run format`, `npm run lint` (0 erreur ; warning `react-hooks/incompatible-library` pré-existant toléré), `npm run test` au vert.
- Code touchant Prisma/types : `DATABASE_URL="postgresql://build:build@localhost:5432/build?schema=public" npm run build` avant de committer.

## File Structure

**Créés :**
- `src/i18n/locale.ts` — constantes locales + `isLocale` + `resolveLocale`. Responsabilité : résolution de la locale active.
- `src/i18n/request.ts` — `getRequestConfig` next-intl. Responsabilité : fournir `{locale, messages}` par requête.
- `src/locales/fr.json`, `src/locales/en.json` — catalogues. Source de vérité = `fr.json`.
- `src/app/actions/locale.ts` — server action `setLocaleAction`.
- `src/components/settings/locale-toggle.tsx` — sélecteur FR/EN.
- `src/test/render-with-intl.tsx` — helper de test (wrap `NextIntlClientProvider` + `fr.json`).
- `src/i18n/locale.test.ts`, `src/app/actions/locale.test.ts`, `src/components/settings/locale-toggle.test.tsx`, `src/locales/parity.test.ts`.

**Modifiés :**
- `next.config.ts` — plugin next-intl.
- `src/app/layout.tsx` — Server Component async, provider, `lang` dynamique, `generateMetadata`.
- `src/app/(main)/settings/page.tsx` — `<LocaleToggle />`.
- `src/lib/categories.ts`, `src/lib/subcategories.ts` — retrait des `label`, helpers ajustés.
- `src/lib/categories.test.ts`, `src/lib/subcategories.label.test.ts` — adaptés.
- Tous les `.tsx` visibles + call sites de `.label`/`subcategoryLabel`/formatters — extraction `t(...)` / locale active.

---

## Phase 0 — Infrastructure next-intl

### Task 1: Dépendance, plugin, résolution de locale

**Files:**
- Modify: `next.config.ts`
- Create: `src/i18n/locale.ts`, `src/i18n/request.ts`, `src/locales/fr.json`, `src/locales/en.json`
- Test: `src/i18n/locale.test.ts`

- [ ] **Step 1: Installer next-intl**

Run: `npm install next-intl@^4`
Expected: `next-intl` ajouté à `dependencies` dans `package.json`.

- [ ] **Step 2: Créer les catalogues initiaux**

`src/locales/fr.json` :
```json
{
  "common": {
    "appName": "kōza",
    "appTagline": "Suivi de budget personnel — 50 / 30 / 20"
  }
}
```
`src/locales/en.json` :
```json
{
  "common": {
    "appName": "kōza",
    "appTagline": "Personal budget tracker — 50 / 30 / 20"
  }
}
```

- [ ] **Step 3: Écrire le test de `resolveLocale` (échoue)**

`src/i18n/locale.test.ts` :
```ts
// @vitest-environment node
import { describe, it, expect, vi, beforeEach } from "vitest";

const get = vi.fn();
vi.mock("next/headers", () => ({ cookies: () => Promise.resolve({ get }) }));
vi.mock("@/lib/prisma", () => ({
  prisma: { userSettings: { findUnique: vi.fn() } },
}));

import { resolveLocale, isLocale } from "@/i18n/locale";
import { prisma } from "@/lib/prisma";

describe("isLocale", () => {
  it("accepts known locales only", () => {
    expect(isLocale("fr")).toBe(true);
    expect(isLocale("en")).toBe(true);
    expect(isLocale("de")).toBe(false);
    expect(isLocale(undefined)).toBe(false);
  });
});

describe("resolveLocale", () => {
  beforeEach(() => vi.clearAllMocks());

  it("uses a valid cookie first", async () => {
    get.mockReturnValue({ value: "en" });
    expect(await resolveLocale()).toBe("en");
    expect(prisma.userSettings.findUnique).not.toHaveBeenCalled();
  });

  it("falls back to the DB locale when the cookie is missing", async () => {
    get.mockReturnValue(undefined);
    vi.mocked(prisma.userSettings.findUnique).mockResolvedValue({ locale: "en" } as never);
    expect(await resolveLocale()).toBe("en");
  });

  it("falls back to the default when the cookie is invalid and no DB row", async () => {
    get.mockReturnValue({ value: "de" });
    vi.mocked(prisma.userSettings.findUnique).mockResolvedValue(null as never);
    expect(await resolveLocale()).toBe("fr");
  });
});
```

- [ ] **Step 4: Lancer** `npm run test -- src/i18n/locale.test.ts` → FAIL (module introuvable).

- [ ] **Step 5: Implémenter `src/i18n/locale.ts`**

```ts
import { cookies } from "next/headers";
import { prisma } from "@/lib/prisma";

export const LOCALES = ["fr", "en"] as const;
export type Locale = (typeof LOCALES)[number];
export const DEFAULT_LOCALE: Locale = "fr";
export const LOCALE_COOKIE = "NEXT_LOCALE";
const SETTINGS_ID = "default";

export function isLocale(value: unknown): value is Locale {
  return typeof value === "string" && (LOCALES as readonly string[]).includes(value);
}

// Locale active : cookie NEXT_LOCALE (rapide), sinon préférence DB, sinon défaut.
export async function resolveLocale(): Promise<Locale> {
  const cookieStore = await cookies();
  const fromCookie = cookieStore.get(LOCALE_COOKIE)?.value;
  if (isLocale(fromCookie)) return fromCookie;

  const settings = await prisma.userSettings.findUnique({ where: { id: SETTINGS_ID } });
  if (isLocale(settings?.locale)) return settings.locale;

  return DEFAULT_LOCALE;
}
```

- [ ] **Step 6: Implémenter `src/i18n/request.ts`**

```ts
import { getRequestConfig } from "next-intl/server";
import { resolveLocale } from "@/i18n/locale";

export default getRequestConfig(async () => {
  const locale = await resolveLocale();
  const messages = (await import(`../locales/${locale}.json`)).default;
  return { locale, messages };
});
```

- [ ] **Step 7: Brancher le plugin dans `next.config.ts`**

```ts
import type { NextConfig } from "next";
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");

const nextConfig: NextConfig = {
  output: "standalone",
};

export default withNextIntl(nextConfig);
```

- [ ] **Step 8: Lancer** `npm run test -- src/i18n/locale.test.ts` → PASS (4 tests).

- [ ] **Step 9: Build + commit**
```bash
DATABASE_URL="postgresql://build:build@localhost:5432/build?schema=public" npm run build
npm run format && npm run lint
git add next.config.ts src/i18n src/locales package.json package-lock.json
git commit -m "feat(i18n): wire next-intl with cookie/db locale resolution"
```

---

### Task 2: Provider racine, `lang` dynamique, helper de test, parité des clés

**Files:**
- Modify: `src/app/layout.tsx`
- Create: `src/test/render-with-intl.tsx`, `src/locales/parity.test.ts`

- [ ] **Step 1: Écrire le test de parité (échoue à terme, passe ici car catalogues identiques)**

`src/locales/parity.test.ts` :
```ts
// @vitest-environment node
import { describe, it, expect } from "vitest";
import fr from "@/locales/fr.json";
import en from "@/locales/en.json";

// Aplatit les clés imbriquées en chemins "a.b.c".
function keyPaths(obj: Record<string, unknown>, prefix = ""): string[] {
  return Object.entries(obj).flatMap(([key, value]) => {
    const path = prefix ? `${prefix}.${key}` : key;
    return value && typeof value === "object" && !Array.isArray(value)
      ? keyPaths(value as Record<string, unknown>, path)
      : [path];
  });
}

describe("locale catalogs", () => {
  it("fr and en expose the exact same key set", () => {
    expect(keyPaths(fr).sort()).toEqual(keyPaths(en).sort());
  });
});
```

- [ ] **Step 2: Lancer** `npm run test -- src/locales/parity.test.ts` → PASS (catalogues alignés). Garde-fou pour la suite.

- [ ] **Step 3: Créer le helper `src/test/render-with-intl.tsx`**

```tsx
import type { ReactElement } from "react";
import { render } from "@testing-library/react";
import { NextIntlClientProvider } from "next-intl";
import frMessages from "@/locales/fr.json";

// Rend un composant client en fournissant le catalogue FR réel,
// afin que les assertions sur les textes français restent valides.
export function renderWithIntl(ui: ReactElement) {
  return render(
    <NextIntlClientProvider locale="fr" messages={frMessages}>
      {ui}
    </NextIntlClientProvider>,
  );
}
```

- [ ] **Step 4: Transformer le layout racine** — `src/app/layout.tsx` :

```tsx
import type { Metadata } from "next";
import { Inter, Instrument_Serif } from "next/font/google";
import { NextIntlClientProvider } from "next-intl";
import { getLocale, getMessages, getTranslations } from "next-intl/server";
import { ThemeProvider } from "@/components/theme-provider";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const instrumentSerif = Instrument_Serif({
  variable: "--font-instrument",
  weight: "400",
  subsets: ["latin"],
});

export async function generateMetadata(): Promise<Metadata> {
  const t = await getTranslations("common");
  return {
    title: t("appName"),
    description: t("appTagline"),
  };
}

export default async function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  const locale = await getLocale();
  const messages = await getMessages();

  return (
    <html
      lang={locale}
      suppressHydrationWarning
      className={`${inter.variable} ${instrumentSerif.variable} h-full`}
    >
      <body className="min-h-full">
        <NextIntlClientProvider locale={locale} messages={messages}>
          <ThemeProvider attribute="class" defaultTheme="light" enableSystem={false}>
            {children}
          </ThemeProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 5: Build + commit**
```bash
DATABASE_URL="postgresql://build:build@localhost:5432/build?schema=public" npm run build
npm run format && npm run lint
git add src/app/layout.tsx src/test/render-with-intl.tsx src/locales/parity.test.ts
git commit -m "feat(i18n): provide intl context and dynamic html lang"
```

---

## Phase 1 — Changement de langue

### Task 3: Server action `setLocaleAction`

**Files:**
- Create: `src/app/actions/locale.ts`, `src/app/actions/locale.test.ts`

- [ ] **Step 1: Écrire le test (échoue)**

`src/app/actions/locale.test.ts` :
```ts
// @vitest-environment node
import { describe, it, expect, vi, beforeEach } from "vitest";

const set = vi.fn();
vi.mock("next/headers", () => ({ cookies: () => Promise.resolve({ set }) }));
vi.mock("@/lib/prisma", () => ({
  prisma: { userSettings: { upsert: vi.fn() } },
}));

import { setLocaleAction } from "@/app/actions/locale";
import { prisma } from "@/lib/prisma";

describe("setLocaleAction", () => {
  beforeEach(() => vi.clearAllMocks());

  it("persists a valid locale to the DB and the cookie", async () => {
    await setLocaleAction("en");
    expect(prisma.userSettings.upsert).toHaveBeenCalledWith(
      expect.objectContaining({
        where: { id: "default" },
        update: { locale: "en" },
        create: { id: "default", locale: "en" },
      }),
    );
    expect(set).toHaveBeenCalledWith("NEXT_LOCALE", "en", expect.objectContaining({ path: "/" }));
  });

  it("ignores an invalid locale", async () => {
    await setLocaleAction("de" as never);
    expect(prisma.userSettings.upsert).not.toHaveBeenCalled();
    expect(set).not.toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Lancer** → FAIL.

- [ ] **Step 3: Implémenter `src/app/actions/locale.ts`**

```ts
"use server";

import { cookies } from "next/headers";
import { prisma } from "@/lib/prisma";
import { isLocale, LOCALE_COOKIE, type Locale } from "@/i18n/locale";

const ONE_YEAR = 60 * 60 * 24 * 365;

// Écrit la préférence de langue (DB durable + cookie de rendu). No-op si locale inconnue.
export async function setLocaleAction(locale: Locale): Promise<void> {
  if (!isLocale(locale)) return;

  await prisma.userSettings.upsert({
    where: { id: "default" },
    update: { locale },
    create: { id: "default", locale },
  });

  const cookieStore = await cookies();
  cookieStore.set(LOCALE_COOKIE, locale, { path: "/", maxAge: ONE_YEAR });
}
```

- [ ] **Step 4: Lancer** → PASS (2 tests).

- [ ] **Step 5: Commit**
```bash
npm run format && npm run lint
git add src/app/actions/locale.ts src/app/actions/locale.test.ts
git commit -m "feat(i18n): add setLocale server action"
```

---

### Task 4: `LocaleToggle` + intégration Réglages

**Files:**
- Create: `src/components/settings/locale-toggle.tsx`, `src/components/settings/locale-toggle.test.tsx`
- Modify: `src/app/(main)/settings/page.tsx`, `src/locales/fr.json`, `src/locales/en.json`

- [ ] **Step 1: Ajouter le namespace `settings` aux catalogues**

Dans `src/locales/fr.json`, ajouter au niveau racine :
```json
  "settings": {
    "title": "Réglages",
    "appearance": "Apparence",
    "darkTheme": "Thème sombre",
    "language": "Langue",
    "incomes": "Revenus",
    "manageIncomes": "Gérer mes revenus",
    "data": "Données",
    "exportData": "Exporter mes données (JSON)"
  }
```
Dans `src/locales/en.json`, ajouter :
```json
  "settings": {
    "title": "Settings",
    "appearance": "Appearance",
    "darkTheme": "Dark theme",
    "language": "Language",
    "incomes": "Income",
    "manageIncomes": "Manage my income",
    "data": "Data",
    "exportData": "Export my data (JSON)"
  }
```

- [ ] **Step 2: Écrire le test du toggle (échoue)**

`src/components/settings/locale-toggle.test.tsx` :
```tsx
import { describe, it, expect, vi, beforeEach } from "vitest";
import { screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { renderWithIntl } from "@/test/render-with-intl";

const refresh = vi.fn();
const setLocaleAction = vi.fn().mockResolvedValue(undefined);
vi.mock("next/navigation", () => ({ useRouter: () => ({ refresh }) }));
vi.mock("@/app/actions/locale", () => ({ setLocaleAction }));
vi.mock("next-intl", async (importOriginal) => {
  const actual = await importOriginal<typeof import("next-intl")>();
  return { ...actual, useLocale: () => "fr" };
});

import { LocaleToggle } from "@/components/settings/locale-toggle";

describe("LocaleToggle", () => {
  beforeEach(() => {
    refresh.mockClear();
    setLocaleAction.mockClear();
  });

  it("switches locale and refreshes", async () => {
    renderWithIntl(<LocaleToggle />);
    await userEvent.click(screen.getByRole("button", { name: "EN" }));
    await waitFor(() => expect(setLocaleAction).toHaveBeenCalledWith("en"));
    await waitFor(() => expect(refresh).toHaveBeenCalledOnce());
  });
});
```

- [ ] **Step 3: Lancer** → FAIL.

- [ ] **Step 4: Implémenter `src/components/settings/locale-toggle.tsx`**

```tsx
"use client";

import { useTransition } from "react";
import { useLocale } from "next-intl";
import { useRouter } from "next/navigation";
import { Segmented } from "@/components/ui/segmented";
import { setLocaleAction } from "@/app/actions/locale";
import { LOCALES, type Locale } from "@/i18n/locale";

export function LocaleToggle() {
  const locale = useLocale();
  const router = useRouter();
  const [, startTransition] = useTransition();

  return (
    <Segmented
      options={LOCALES.map((value) => ({ value, label: value.toUpperCase() }))}
      value={locale}
      onChange={(next) =>
        startTransition(async () => {
          await setLocaleAction(next as Locale);
          router.refresh();
        })
      }
    />
  );
}
```

- [ ] **Step 5: Lancer** → PASS.

- [ ] **Step 6: Câbler la page Réglages** — `src/app/(main)/settings/page.tsx` :

```tsx
import Link from "next/link";
import { getTranslations } from "next-intl/server";
import { ThemeToggle } from "@/components/settings/theme-toggle";
import { LocaleToggle } from "@/components/settings/locale-toggle";

export const dynamic = "force-dynamic";

export default async function SettingsPage() {
  const t = await getTranslations("settings");

  return (
    <main className="mx-auto flex min-h-screen max-w-[720px] flex-col gap-10 px-6 py-12">
      <h1 className="font-serif text-[28px] leading-tight text-text">{t("title")}</h1>

      <section className="flex flex-col gap-4">
        <h2 className="text-[13px] font-medium uppercase tracking-wide text-text-secondary">
          {t("appearance")}
        </h2>
        <div className="card flex items-center justify-between p-5">
          <span className="text-[15px] text-text">{t("darkTheme")}</span>
          <ThemeToggle />
        </div>
      </section>

      <section className="flex flex-col gap-4">
        <h2 className="text-[13px] font-medium uppercase tracking-wide text-text-secondary">
          {t("language")}
        </h2>
        <div className="card flex items-center justify-between p-5">
          <span className="text-[15px] text-text">{t("language")}</span>
          <LocaleToggle />
        </div>
      </section>

      <section className="flex flex-col gap-4">
        <h2 className="text-[13px] font-medium uppercase tracking-wide text-text-secondary">
          {t("incomes")}
        </h2>
        <Link href="/incomes" className="text-[14px] font-medium text-accent">
          {t("manageIncomes")}
        </Link>
      </section>

      <section className="flex flex-col gap-4">
        <h2 className="text-[13px] font-medium uppercase tracking-wide text-text-secondary">
          {t("data")}
        </h2>
        <a href="/api/export" download className="text-[14px] font-medium text-accent">
          {t("exportData")}
        </a>
      </section>
    </main>
  );
}
```

- [ ] **Step 7: Build + commit**
```bash
DATABASE_URL="postgresql://build:build@localhost:5432/build?schema=public" npm run build
npm run format && npm run lint
git add src/components/settings/locale-toggle.tsx src/components/settings/locale-toggle.test.tsx "src/app/(main)/settings/page.tsx" src/locales
git commit -m "feat(settings): add working language toggle"
```

---

## Phase 2 — Formatters & libellés dérivés

### Task 5: Migration des libellés catégories/sous-catégories vers les catalogues

**Files:**
- Modify: `src/lib/categories.ts`, `src/lib/subcategories.ts`, `src/lib/categories.test.ts`, `src/lib/subcategories.label.test.ts`, `src/locales/fr.json`, `src/locales/en.json`
- Modify (call sites): voir Step 5

- [ ] **Step 1: Ajouter les namespaces `categories` et `subcategories` aux catalogues**

`src/locales/fr.json` (racine) :
```json
  "categories": {
    "essential": "Essentiels",
    "leisure": "Loisirs",
    "savings": "Épargne"
  },
  "subcategories": {
    "housing": "Logement",
    "food": "Alimentation",
    "transport": "Transports",
    "health": "Santé",
    "insurance": "Assurances",
    "bills": "Factures",
    "restaurants": "Restaurants",
    "outings": "Sorties",
    "vacations": "Vacances",
    "sport": "Sport",
    "games": "Jeux vidéo",
    "culture": "Culture",
    "savings_account": "Livret d'épargne",
    "etf": "ETF",
    "stocks": "Actions",
    "real_estate": "Immobilier",
    "emergency_fund": "Fonds d'urgence"
  }
```
`src/locales/en.json` (racine) :
```json
  "categories": {
    "essential": "Essentials",
    "leisure": "Leisure",
    "savings": "Savings"
  },
  "subcategories": {
    "housing": "Housing",
    "food": "Food",
    "transport": "Transport",
    "health": "Health",
    "insurance": "Insurance",
    "bills": "Bills",
    "restaurants": "Restaurants",
    "outings": "Outings",
    "vacations": "Holidays",
    "sport": "Sport",
    "games": "Video games",
    "culture": "Culture",
    "savings_account": "Savings account",
    "etf": "ETFs",
    "stocks": "Stocks",
    "real_estate": "Real estate",
    "emergency_fund": "Emergency fund"
  }
```

- [ ] **Step 2: Retirer les `label` de `src/lib/categories.ts`**

Supprimer le champ `label` de `CategoryConfig` et de chaque entrée de `CATEGORIES`. Résultat :
```ts
export type CategoryKey = "essential" | "leisure" | "savings";

export interface CategoryConfig {
  key: CategoryKey;
  share: number;
  dotClass: string;
  textClass: string;
  bgClass: string;
}

export const CATEGORIES: Record<CategoryKey, CategoryConfig> = {
  essential: {
    key: "essential",
    share: 0.5,
    dotClass: "bg-essential",
    textClass: "text-essential",
    bgClass: "bg-essential-bg",
  },
  leisure: {
    key: "leisure",
    share: 0.3,
    dotClass: "bg-leisure",
    textClass: "text-leisure",
    bgClass: "bg-leisure-bg",
  },
  savings: {
    key: "savings",
    share: 0.2,
    dotClass: "bg-savings",
    textClass: "text-savings",
    bgClass: "bg-savings-bg",
  },
};

export const CATEGORY_ORDER: CategoryKey[] = ["essential", "leisure", "savings"];
```

- [ ] **Step 3: Retirer les `label` de `src/lib/subcategories.ts`**

Supprimer le champ `label` de `SubcategoryConfig` et de chaque entrée. Supprimer la fonction `subcategoryLabel` (remplacée par `t("subcategories.<key>")` aux call sites). Résultat :
```ts
import { CATEGORY_ORDER, type CategoryKey } from "@/lib/categories";

export interface SubcategoryConfig {
  key: string;
  category: CategoryKey;
}

export const SUBCATEGORIES: Record<CategoryKey, SubcategoryConfig[]> = {
  essential: [
    { key: "housing", category: "essential" },
    { key: "food", category: "essential" },
    { key: "transport", category: "essential" },
    { key: "health", category: "essential" },
    { key: "insurance", category: "essential" },
    { key: "bills", category: "essential" },
  ],
  leisure: [
    { key: "restaurants", category: "leisure" },
    { key: "outings", category: "leisure" },
    { key: "vacations", category: "leisure" },
    { key: "sport", category: "leisure" },
    { key: "games", category: "leisure" },
    { key: "culture", category: "leisure" },
  ],
  savings: [
    { key: "savings_account", category: "savings" },
    { key: "etf", category: "savings" },
    { key: "stocks", category: "savings" },
    { key: "real_estate", category: "savings" },
    { key: "emergency_fund", category: "savings" },
  ],
};

export const ALL_SUBCATEGORIES: SubcategoryConfig[] = CATEGORY_ORDER.flatMap(
  (key) => SUBCATEGORIES[key],
);

export const SUBCATEGORY_KEYS: Set<string> = new Set(ALL_SUBCATEGORIES.map((sub) => sub.key));

export function isValidSubcategory(category: CategoryKey, key: string): boolean {
  return SUBCATEGORIES[category].some((sub) => sub.key === key);
}

// Clé de la première sous-catégorie d'une catégorie (chaque catégorie en a au moins une).
export function defaultSubcategory(category: CategoryKey): string {
  const [first] = SUBCATEGORIES[category];
  return first?.key ?? "";
}
```

- [ ] **Step 4: Adapter les tests lib**

Dans `src/lib/categories.test.ts` : retirer toute assertion sur `.label` (ex. `expect(CATEGORIES.essential.label).toBe("Essentiels")`). Conserver les assertions sur `share`/`key`/ordre.
Dans `src/lib/subcategories.label.test.ts` : ce fichier teste `subcategoryLabel` (supprimé). Le remplacer intégralement par un test des clés :
```ts
// @vitest-environment node
import { describe, it, expect } from "vitest";
import { SUBCATEGORIES, isValidSubcategory, defaultSubcategory } from "@/lib/subcategories";

describe("subcategories", () => {
  it("validates membership by category", () => {
    expect(isValidSubcategory("essential", "housing")).toBe(true);
    expect(isValidSubcategory("leisure", "housing")).toBe(false);
  });

  it("returns the first key as default", () => {
    expect(defaultSubcategory("essential")).toBe("housing");
    expect(defaultSubcategory("savings")).toBe("savings_account");
  });

  it("every subcategory carries its parent category", () => {
    for (const [category, subs] of Object.entries(SUBCATEGORIES)) {
      for (const sub of subs) expect(sub.category).toBe(category);
    }
  });
});
```

- [ ] **Step 5: Mettre à jour les call sites de `.label` / `subcategoryLabel`**

Run pour les lister :
```bash
rtk grep -nE "\.label|subcategoryLabel|CATEGORIES\[.*\]\.label" src/components src/app
```
Pour **chaque** occurrence :
- Composant **client** : ajouter `const tc = useTranslations("categories")` / `const ts = useTranslations("subcategories")` et remplacer `CATEGORIES[cat].label` → `tc(cat)`, `subcategoryLabel(key)` → `ts(key)`.
- Composant **server** : `const tc = await getTranslations("categories")` / `getTranslations("subcategories")`, idem.

Fichiers connus consommant ces libellés (à vérifier via le grep ci-dessus) : `src/components/ui/cat-select.tsx`, `src/components/expenses/subcat-chips.tsx`, `src/components/expenses/expense-row.tsx`, `src/components/dashboard/category-progress-card.tsx`, `src/components/charts/category-donut.tsx`, `src/components/budget/envelopes-summary.tsx`, `src/components/budgets/budget-card.tsx`, `src/components/incomes/income-row.tsx`. Traiter chacun.

- [ ] **Step 6: Lancer la suite ciblée**
```bash
npm run test -- src/lib/categories.test.ts src/lib/subcategories.label.test.ts
DATABASE_URL="postgresql://build:build@localhost:5432/build?schema=public" npm run build
```
Expected: tests verts ; build vert (aucune référence résiduelle à `.label`/`subcategoryLabel` — sinon le typecheck échoue, corriger).

- [ ] **Step 7: Commit**
```bash
npm run format && npm run lint
git add src/lib/categories.ts src/lib/subcategories.ts src/lib/categories.test.ts src/lib/subcategories.label.test.ts src/locales src/components src/app
git commit -m "feat(i18n): move category and subcategory labels to catalogs"
```

---

### Task 6: Alimenter les formatters avec la locale active

**Files:** Modify — tous les composants/pages appelant `formatEUR`/`formatDate`/`formatMonth`.

- [ ] **Step 1: Lister les appels**

Run:
```bash
rtk grep -nE "formatEUR|formatDate|formatMonth" src/components src/app
```

- [ ] **Step 2: Passer la locale active à chaque appel**

- **Server Component** : en tête, `const locale = await getLocale();` (`import { getLocale } from "next-intl/server"`), puis `formatEUR(value, locale)`, `formatMonth(month, locale)`, etc.
- **Client Component** : `const locale = useLocale() as "fr" | "en";` (`import { useLocale } from "next-intl"`), puis passer `locale` aux appels.

Le 2ᵉ paramètre est déjà typé `"fr" | "en"` ; `useLocale()`/`getLocale()` renvoient `string`, caster en `"fr" | "en"` (la locale est garantie valide par `resolveLocale`).

- [ ] **Step 3: Build (typecheck)**
```bash
DATABASE_URL="postgresql://build:build@localhost:5432/build?schema=public" npm run build
```
Expected: vert. `npm run test` : les tests de formatters (`src/lib/formatters*.test.ts`) restent verts (signature inchangée).

- [ ] **Step 4: Commit**
```bash
npm run format && npm run lint
git add src/components src/app
git commit -m "feat(i18n): format amounts and dates with the active locale"
```

---

## Phase 3 — Extraction des chaînes par domaine

> **Recette d'extraction (appliquée à chaque tâche de cette phase) :**
> 1. Lire chaque fichier listé, relever toute chaîne visible en dur (texte JSX, `aria-label`, `placeholder`, `title`, `label` de props, messages d'erreur affichés).
> 2. Ajouter les clés sous le namespace du domaine dans `fr.json` (texte FR existant **à l'identique**) **et** `en.json` (traduction). Valeurs dynamiques → interpolation ICU `"{name}"` ; pluriels → `"{count, plural, one {# élément} other {# éléments}}"`.
> 3. Remplacer dans le code : Client → `const t = useTranslations("<ns>")` + `t("key")` ; Server → `const t = await getTranslations("<ns>")`.
> 4. Tests composants existants du domaine : remplacer `render(...)` par `renderWithIntl(...)` (import depuis `@/test/render-with-intl`). Les assertions FR restent valides (le helper charge `fr.json`).
> 5. `npm run test -- <dossier du domaine>` vert, puis build, puis commit.
> Le test de parité `src/locales/parity.test.ts` garantit que chaque clé ajoutée en FR existe aussi en EN (sinon CI rouge).

### Task 7: Navigation + commun + onboarding

**Files:** Modify
- `src/components/nav/app-nav.tsx`, `src/app/(main)/layout.tsx`, `src/app/(onboarding)/layout.tsx`, `src/app/page.tsx`
- `src/app/(onboarding)/welcome/page.tsx`, `src/app/(onboarding)/setup/page.tsx`, `src/app/(onboarding)/confirm/page.tsx`
- `src/components/onboarding/income-setup-form.tsx`, `src/components/onboarding/confirm-actions.tsx`, `src/components/onboarding/step-indicator.tsx`
- `src/app/(dev)/kit/page.tsx` (page de dev : extraire aussi, ou laisser tel quel — décision : laisser, hors UI produit)
- Catalogues : namespaces `nav`, `onboarding` (+ compléments `common`)
- Tests : `src/components/onboarding/income-setup-form.test.tsx` et tout test d'onboarding existant → `renderWithIntl`.

- [ ] **Step 1:** Appliquer la recette d'extraction aux fichiers ci-dessus (sauf `kit/page.tsx`). Exemple de clés `nav` à créer (libellés d'état actif de `app-nav.tsx`) : `nav.dashboard`, `nav.expenses`, `nav.budgets`, `nav.settings`.
- [ ] **Step 2:** `npm run test -- src/components/onboarding src/components/nav` → PASS.
- [ ] **Step 3:** Build vert.
- [ ] **Step 4:** Commit `git commit -m "feat(i18n): translate navigation and onboarding"`.

### Task 8: Dashboard

**Files:** Modify
- `src/app/(main)/dashboard/page.tsx`
- `src/components/dashboard/carry-line.tsx`, `category-progress-card.tsx`, `dashboard-month-nav.tsx`, `prev-month-delta.tsx`
- `src/components/charts/category-donut.tsx`, `src/components/budget/envelopes-summary.tsx`
- Catalogue : namespace `dashboard`
- Tests : `carry-line.test.tsx`, `category-progress-card.test.tsx`, etc. → `renderWithIntl`.

> Attention `carry-line.tsx` : le texte « Report du mois dernier : +X … » devient une clé ICU, ex. `"dashboard.carryPositive": "Report du mois dernier : +{amount}"` et `"dashboard.carryNegative": "Report du mois dernier : {amount} à absorber"`. Le test `carry-line.test.tsx` (assertions `/report du mois dernier/i` + `/600,00/`) reste vert via `renderWithIntl`.

- [ ] **Step 1:** Recette d'extraction sur les fichiers ci-dessus.
- [ ] **Step 2:** `npm run test -- src/components/dashboard src/components/charts src/components/budget` → PASS.
- [ ] **Step 3:** Build vert.
- [ ] **Step 4:** Commit `git commit -m "feat(i18n): translate dashboard"`.

### Task 9: Revenus

**Files:** Modify
- `src/app/(main)/incomes/page.tsx`
- `src/components/incomes/income-form.tsx`, `income-row.tsx`, `incomes-manager.tsx`
- Catalogue : namespace `incomes`
- Tests : `src/components/incomes/*.test.tsx` → `renderWithIntl`.

- [ ] **Step 1:** Recette d'extraction. - [ ] **Step 2:** `npm run test -- src/components/incomes` PASS. - [ ] **Step 3:** Build vert. - [ ] **Step 4:** Commit `git commit -m "feat(i18n): translate incomes"`.

### Task 10: Dépenses

**Files:** Modify
- `src/app/(main)/expenses/page.tsx`
- `src/components/expenses/expense-quick-form.tsx`, `expense-row.tsx`, `expenses-manager.tsx`, `subcat-chips.tsx`
- Catalogue : namespace `expenses`
- Tests : `src/components/expenses/*.test.tsx` → `renderWithIntl`.

- [ ] **Step 1:** Recette d'extraction. - [ ] **Step 2:** `npm run test -- src/components/expenses` PASS. - [ ] **Step 3:** Build vert. - [ ] **Step 4:** Commit `git commit -m "feat(i18n): translate expenses"`.

### Task 11: Budgets

**Files:** Modify
- `src/app/(main)/budgets/page.tsx`
- `src/components/budgets/budget-form.tsx`, `budget-card.tsx`, `budgets-manager.tsx`
- Catalogue : namespace `budgets`
- Tests : `src/components/budgets/*.test.tsx` → `renderWithIntl`.

- [ ] **Step 1:** Recette d'extraction. - [ ] **Step 2:** `npm run test -- src/components/budgets` PASS. - [ ] **Step 3:** Build vert. - [ ] **Step 4:** Commit `git commit -m "feat(i18n): translate budgets"`.

### Task 12: Récurrentes

**Files:** Modify
- `src/app/(main)/recurring/page.tsx`
- `src/components/recurring/recurring-form.tsx`, `recurring-manager.tsx`, `pending-confirmations.tsx`
- Catalogue : namespace `recurring`
- Tests : `src/components/recurring/*.test.tsx` → `renderWithIntl`.

- [ ] **Step 1:** Recette d'extraction. - [ ] **Step 2:** `npm run test -- src/components/recurring` PASS. - [ ] **Step 3:** Build vert. - [ ] **Step 4:** Commit `git commit -m "feat(i18n): translate recurring expenses"`.

### Task 13: Composants UI restants + balayage final

**Files:** Modify
- `src/components/ui/*.tsx` portant des chaînes par défaut visibles (vérifier `confirm-dialog.tsx`, `month-nav.tsx`, `overlay.tsx`, `field.tsx`) — extraire toute chaîne par défaut affichée (ex. libellés de confirmation/annulation) vers `common`.
- Catalogue : compléments `common` (ex. `common.cancel`, `common.confirm`, `common.delete`, `common.save`, `common.close`).
- Tests UI concernés → `renderWithIntl` si la chaîne par défaut est testée.

- [ ] **Step 1: Balayage exhaustif** — détecter les chaînes FR résiduelles :
```bash
rtk grep -rnE "[A-Za-zÀ-ÿ]{3,}" src/components src/app --include=*.tsx | rtk grep -vE "className|import|aria-|data-|useTranslations|getTranslations|from \"|//|t\(\"" | rtk grep -E "[éèàùçÉÈÀ]| (le|la|les|une|un|des|mes|mois) "
```
Inspecter chaque résultat ; extraire ce qui est visible et non encore traduit.
- [ ] **Step 2:** `npm run test` (suite complète) → PASS.
- [ ] **Step 3:** Build vert.
- [ ] **Step 4:** Commit `git commit -m "feat(i18n): translate shared ui strings"`.

---

## Phase 4 — Vérification & PR

### Task 14: Vérification finale

- [ ] **Step 1: Suite complète + lint + build**
```bash
npm run lint && DATABASE_URL="postgresql://build:build@localhost:5432/build?schema=public" npm run build && npm run test
```
Expected: lint clean (warnings RHF pré-existants tolérés), build vert (toutes routes), tous tests verts incluant `parity.test.ts`.

- [ ] **Step 2: [DB — utilisateur] Vérification manuelle**

`npm run dev`, ouvrir `http://localhost:3000` :
- Réglages → basculer **FR/EN** : toute l'app change de langue (nav, dashboard, formulaires) ; rechargement → la langue persiste (cookie + DB).
- Montants : `1 234,56 €` en FR / `€1,234.56` en EN. Dates : `JJ/MM/AAAA` / `MM/DD/YYYY`.
- Catégories/sous-catégories traduites partout (dashboard, formulaires, listes).
- Vérifier light + dark, mobile + desktop. Arrêter le serveur.

### Task 15: Mettre à jour le CLAUDE.md

- [ ] **Step 1:** Section « Internationalisation » : préciser l'implémentation retenue (next-intl 4 sans routing, résolution cookie→DB→`fr`, catalogues `src/locales/{fr,en}.json`, toggle persistant). Section « Settings » : le sélecteur de langue est fonctionnel.
- [ ] **Step 2:** Commit `git add CLAUDE.md && git commit -m "docs(claude): document i18n implementation"`.

### Task 16: Push & PR

- [ ] **Step 1:**
```bash
git push -u origin feat/i18n
gh pr create --base main --head feat/i18n \
  --title "feat(i18n): add full FR/EN internationalisation" \
  --body "Câble next-intl 4 sans routing (locale résolue cookie NEXT_LOCALE → DB UserSettings.locale → défaut fr), extrait toutes les chaînes visibles vers src/locales/{fr,en}.json (FR + EN complets), et ajoute un sélecteur de langue persistant (server action DB + cookie, router.refresh). Formatters alimentés par la locale active. Libellés catégories/sous-catégories déplacés dans les catalogues. Test de parité des clés fr/en en garde-fou."
```
- [ ] **Step 2:** Ne pas merger — le mainteneur merge une fois la CI verte.

---

## Self-Review

**Couverture du spec :**
- Mode next-intl sans routing (§Décisions) → Task 1 ✅
- Résolution cookie→DB→défaut (§Résolution) → Task 1 (`resolveLocale` + test) ✅
- Toggle DB+cookie+refresh (§Application) → Tasks 3, 4 ✅
- Provider racine + `lang` dynamique + metadata (§Composants) → Task 2 ✅
- Catalogues `src/locales` namespacés + ICU (§Catalogues) → Tasks 4–13 ✅ · parité des clés → Task 2 ✅
- Formatters locale active (§Formatters) → Task 6 ✅
- Libellés catégories/sous-catégories (§Composants modifiés) → Task 5 ✅
- Extraction des ~55 fichiers (§Extraction) → Tasks 7–13 ✅
- Helper `renderWithIntl` (§Tests) → Task 2 ✅ · tests resolveLocale/setLocale/parité → Tasks 1/3/2 ✅
- Cas limites cookie/DB/clé manquante (§Cas limites) → Tasks 1 + 2 (parité) ✅
- Hors scope (onboarding lang, URL, Accept-Language, notifications) → non couverts, conforme ✅

**Placeholders intentionnels :** les Tasks 7–13 (extraction par domaine) décrivent une **recette appliquée par fichier** plutôt que le diff complet des ~55 fichiers — choix délibéré (les chaînes vivent dans le code, l'exécutant les lit lors de l'extraction). Les clés de catalogue (le contenu réel à traduire) sont fournies en entier pour les namespaces structurants (`common`, `settings`, `categories`, `subcategories`) ; les autres namespaces se remplissent au fil de la lecture des fichiers, avec le test de parité comme filet. Toutes les tâches d'infrastructure (1–6) ont du code complet + commandes + résultats attendus.

**Cohérence des types :**
- `Locale` / `LOCALES` / `isLocale` / `LOCALE_COOKIE` (Task 1) consommés par `resolveLocale` (1), `setLocaleAction` (3), `LocaleToggle` (4).
- `resolveLocale` (1) consommé par `request.ts` (1) ; `getLocale`/`getMessages`/`getTranslations` (next-intl/server) utilisés en layout (2), pages server (4–13), formatters (6).
- `renderWithIntl` (2) consommé par tous les tests composants (4, 7–13).
- Retrait de `label` (Task 5) ⇒ tous les call sites migrés vers `t(...)` dans la même tâche (Step 5) → build vert garanti avant commit.

**Dépendance DB :** seules les vérifs runtime (Task 14 Step 2) nécessitent la DB dev. Tout le reste (tests à Prisma + `next/headers` mockés, build avec `DATABASE_URL` factice) est exécutable sans connexion.
